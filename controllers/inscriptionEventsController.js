const supabase = require('../db/supabase');

// Controller to record a new event
const recordEvent = async (req, res) => {
    // Logic to be added
    res.status(200).send('Events controller is working');
};

module.exports = {
    recordEvent,
};