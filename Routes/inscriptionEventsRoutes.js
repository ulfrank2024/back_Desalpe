const express = require('express');
const router = express.Router();
const inscriptionEventsController = require('../controllers/inscriptionEventsController');

// Route to record a new event
// router.post('/', inscriptionEventsController.recordEvent);

module.exports = router;